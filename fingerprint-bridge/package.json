{
  "name": "fingerprint-bridge",
  "version": "1.0.0",
  "description": "DigitalPersona fingerprint scanner bridge",
  "main": "fingerprint.js",
  "scripts": {
    "build": "node ./node_modules/.bin/node-gyp rebuild",
    "install": "node ./node_modules/.bin/node-gyp rebuild"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  },
  "gypfile": true,
  "devDependencies": {
    "node-gyp": "^11.2.0"
  }
}
